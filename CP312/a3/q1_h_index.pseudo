def Question1_H_index(C):

    left = 0;
    right = n;

    while left <= right Do

        middle = floor((left + right) / 2);

        if C[middle] > middle Do
            left = middle + 1;
        
        else if C[middle] == middle Do
            left = middle;
            right -= 1;
        
        else Do
            right = middle - 1;

    if C[middle] != 0 Do    
        return middle;
    
    else Do
        return -1;

